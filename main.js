
import React from "react";
import ReactDOM from "react-dom/client";
import LumenInstaller from "./LumenInstaller.jsx";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LumenInstaller />
  </React.StrictMode>
);
